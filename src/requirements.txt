# Core
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.0.0

# LangGraph / LangChain
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# Tool calling utilities
langchain-openai>=0.2.0          # provides convert_to_openai_tool helper

# Google Gemini — required when LLM_PROVIDER="gemini" (default)
langchain-google-genai>=2.0.0

# Web search — optional, only needed for general_question routing
# Sign up for a free key at https://tavily.com
langchain-tavily>=0.1.0

# Jupyter — optional, only needed to run the test notebook
jupyterlab>=4.0.0
